import styles from './Opinions.module.scss';

function Opinions() {
  return (
    <section>
      <div className={styles.landingPage}>
        <div className={`container ${styles.homePageContainer}`}></div>
      </div>
    </section>
  );
}

export default Opinions;
